import { View } from 'react-native'
import Clock from './_components/clock'

const ClockScreen = () => {
  return (
    <View style={{flex:1}}>
      <Clock/>
    </View>
  )
}

export default ClockScreen